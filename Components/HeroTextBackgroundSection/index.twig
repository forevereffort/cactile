<section class="hero-text-background-section">
    <div class="container">
        <div class="hero-text-background-inner" style="background-image: url('{{ image.src|resizeDynamic(2000) }}')">
            <div class="content-col">
                {{ content|e('wp_kses_post') }}
                <div class="link-row">
                    <a class="btn btn-dark" href="{{ link_url }}">{{ link_title }}</a>
                </div>
            </div>
        </div>
    </div>
</section>